{
  "_comment": "TEMPLATE ONLY — Do NOT commit real values. Copy these keys into User Secrets or environment variables.",
  "ConnectionStrings": {
    "Default": "Host=<host>;Port=5432;Database=<db>;Username=<user>;Password=<password>;SSL Mode=Require;Trust Server Certificate=true"
  },
  "Jwt": {
    "Key": "<your-jwt-signing-key-min-32-chars>"
  }
}
