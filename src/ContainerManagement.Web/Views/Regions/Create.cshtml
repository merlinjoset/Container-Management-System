@model ContainerManagement.Application.Dtos.Regions.RegionCreateDto

@{
    ViewData["Title"] = "Create Region";
}

<div class="container mt-4">
    <h3 class="fw-bold mb-3">Create Region</h3>

    <form asp-action="Create" method="post">
        @Html.AntiForgeryToken()

        <div asp-validation-summary="All" class="text-danger mb-3"></div>

        <div class="card shadow-sm">
            <div class="card-body">
                <div class="row g-3">
                    <div class="col-md-6">
                        <label asp-for="RegionName" class="form-label"></label>
                        <input asp-for="RegionName" class="form-control" />
                        <span asp-validation-for="RegionName" class="text-danger"></span>
                    </div>
                    <div class="col-md-6">
                        <label asp-for="RegionCode" class="form-label"></label>
                        <input asp-for="RegionCode" class="form-control" />
                        <span asp-validation-for="RegionCode" class="text-danger"></span>
                    </div>
                </div>
            </div>
            <div class="card-footer text-end">
                <a asp-action="Index" class="btn btn-secondary">Cancel</a>
                <button type="submit" class="btn btn-primary">Create</button>
            </div>
        </div>
    </form>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
