@model List<ContainerManagement.Application.Dtos.Vendors.VendorListItemDto>

@{
    ViewData["Title"] = "Vendors";
}

<div class="container mt-4">
    <div class="d-flex flex-column flex-md-row justify-content-between align-items-md-center gap-2 mb-3">
        <h2 class="fw-bold mb-0">Vendors</h2>
        <div class="w-100 w-md-auto" style="min-width:480px;">
            <div class="input-group">
                <span class="input-group-text">Name</span>
                <input id="vendorSearch" class="form-control" placeholder="Filter by name..." />
                <select id="vendorPageSize" class="form-select" style="max-width: 120px;">
                    <option value="10">10</option>
                    <option value="25" selected>25</option>
                    <option value="50">50</option>
                    <option value="0">All</option>
                </select>
                <a asp-action="Create" class="btn btn-primary"><i class="bi bi-plus-lg"></i> Add Vendor</a>
                <a asp-action="Import" class="btn btn-outline-secondary"><i class="bi bi-upload"></i> Import</a>
            </div>
        </div>
    </div>

    <div class="card shadow-sm">
        <div class="card-body">
            <div class="table-responsive">
                <table class="table table-hover table-striped align-middle" id="vendorsTable">
                    <thead class="table-dark">
                        <tr>
                            <th>Vendor Name</th>
                            <th>Vendor Code</th>
                            <th>Country</th>
                            <th class="text-end">Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        @if (Model.Any())
                        {
                            foreach (var v in Model)
                            {
                                <tr>
                                    <td>@v.VendorName</td>
                                    <td>@v.VendorCode</td>
                                    <td>@v.CountryName</td>
                                    <td class="text-end">
                                        <a asp-action="Edit" asp-route-id="@v.Id" class="btn btn-warning me-2">Edit</a>
                                        <button type="button" class="btn btn-danger" data-bs-toggle="modal" data-bs-target="#deleteVendorModal" data-id="@v.Id" data-name="@v.VendorName">
                                            Delete
                                        </button>
                                    </td>
                                </tr>
                            }
                        }
                        else
                        {
                            <tr data-empty-row="1">
                                <td colspan="4" class="text-center text-muted">No vendors available.</td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>

<div class="modal fade" id="deleteVendorModal" tabindex="-1" aria-labelledby="deleteVendorModalLabel" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header bg-danger text-white">
                <h5 class="modal-title" id="deleteVendorModalLabel">Confirm Delete</h5>
                <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                <p class="mb-2">Are you sure you want to delete this vendor?</p>
                <div class="border rounded p-2 bg-light">
                    <div><strong>Vendor Name:</strong> <span id="delVendorName"></span></div>
                </div>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
                <form id="deleteVendorForm" asp-action="Delete" method="post" class="d-inline">
                    @Html.AntiForgeryToken()
                    <input type="hidden" name="id" id="delVendorId" />
                    <button type="submit" class="btn btn-danger">Yes, Delete</button>
                </form>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script>
        const vSearch = document.getElementById('vendorSearch');
        const vTable = document.getElementById('vendorsTable');
        const vSize = document.getElementById('vendorPageSize');
        function applyVendorFilter() {
            const q = (vSearch?.value || '').toLowerCase();
            const rows = Array.from(vTable.querySelectorAll('tbody tr')).filter(r => !r.dataset.emptyRow);
            rows.forEach(tr => {
                const text = tr.innerText.toLowerCase();
                tr.dataset.match = text.includes(q) ? '1' : '0';
            });
            paginateVendors();
        }
        function paginateVendors() {
            const size = parseInt(vSize?.value || '25', 10);
            const matched = Array.from(vTable.querySelectorAll('tbody tr')).filter(tr => tr.dataset.match !== '0' && !tr.dataset.emptyRow);
            const all = Array.from(vTable.querySelectorAll('tbody tr')).filter(r => !r.dataset.emptyRow);
            all.forEach(tr => tr.style.display = 'none');
            if (size === 0) matched.forEach(tr => tr.style.display = '');
            else matched.slice(0, size).forEach(tr => tr.style.display = '');

            const emptyRow = vTable.querySelector('tbody tr[data-empty-row="1"]');
            if (emptyRow) emptyRow.style.display = matched.length === 0 ? '' : 'none';
        }
        vSearch?.addEventListener('input', applyVendorFilter);
        vSize?.addEventListener('change', paginateVendors);
        Array.from(vTable.querySelectorAll('tbody tr')).forEach(tr => { if (!tr.dataset.emptyRow) tr.dataset.match = '1'; });
        paginateVendors();

        const deleteModal = document.getElementById('deleteVendorModal');
        deleteModal.addEventListener('show.bs.modal', function (event) {
            const button = event.relatedTarget;
            const id = button.getAttribute('data-id');
            const name = button.getAttribute('data-name');
            document.getElementById('delVendorId').value = id;
            document.getElementById('delVendorName').textContent = name || '';
        });
    </script>
}
